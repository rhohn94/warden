//! lib-app — reusable library quick-start scaffold (#432).
//!
//! Ships ONE real, doc-tested exemplar module ([`version_compare`])
//! demonstrating the library shape end-to-end: a public API with doc
//! comments carrying runnable examples (`cargo test --doc`), plus unit tests
//! (`cargo test`) and an integration test (`tests/`) that exercises only the
//! crate's PUBLIC surface. Replace this with your project's real public API;
//! keep the doc-tested-example convention as you grow it.

pub mod version_compare;
