# gui-starter

Desktop/GUI application shell: an app entrypoint + a visual-inspection hook, ready for the design-language UX tier.

This is a Grimoire quick-start scaffold for the `gui` profile. Replace this README with your project's. The chosen language variant's commands (test/build/lint/typecheck) are wired by `grm-quick-start-template` on apply. See `docs/design/defaults-quickstart-design.md`.
