# Skill name overrides for sync-from-source.
# Format:  <dest-skill-name>  <source-skill-name>
# Lines without an entry default to identical names.
# Edit this to match how YOUR source project names its skills.

# The scaffolding's generic release skill is the source's project-specific one:
project-release   forge-release
