manifest-version: 64

# Feature manifest

This file is the source of truth for the **sync adoption phase** (F2). It
catalogs every Grimoire framework feature that has an idempotent adoption step —
features an old project might not have enabled yet and that `grm-sync-from-upstream`
should offer (or auto-run under Noir) when the project syncs to a newer release.

Each entry records how to detect whether the feature is already adopted
(`detect`), how to enable it idempotently (`adopt`), and — when user data must
move — an optional explicitly-confirmed migration step (`migrate`). The
`introduced-in` version drives delta computation: features older than the
project's `framework-version` marker are skipped without running `detect`.

**Format:** versioned Markdown table, same pattern as `baseline-requirements.md`.
Bump `manifest-version` whenever a row is **added** or a row's schema changes,
so tooling can detect and merge new entries idempotently.

**Detect-predicate rule (enforced).** A `detect` predicate must reference only
artifacts that are actually **distributed** to a consumer — skills, scripts,
config, gate text. It must **never** depend on a path the v3.39 Bulkhead excludes
from the sync/build (anything under `docs/grimoire/design/` and the other
`docs/grimoire/` framework-internal docs): a consumer never receives those, so the
detect can never pass there and the adoption is silently skipped every sync. The
`grm-doc-assurance manifest-detect-hygiene` check enforces this at release closeout.
Cite an internal design doc in `summary`/`adopt` prose if useful — just not in
`detect`.

| feature-id | introduced-in | summary | detect | adopt | migrate |
|---|---|---|---|---|---|
| `design-scaffold-promotion` | v3.47 | The v3.46 root-only docs-tree scaffolding is promoted to consumers. The three skeleton design subtrees (`docs/design/architecture/`, `docs/design/data-persistence/`, `docs/design/distribution/` — each an index + a house-layout `*-design.md`) plus the `docs/design/README.md` index that lists them, and the `docs/template-files/` static-templates tier (`design-doc-template.md`), are copied into both shipped flavors and the golden bootstrap seed, so fresh projects and synced projects receive the structure like the `ux/` tier. Additive passive scaffolding — no config, no behaviour. The internal-only `docs/grimoire/authoring-grimoire-docs.md` is deliberately NOT promoted. manifest-version 59→60. | Check that `docs/design/architecture/README.md` exists AND `docs/template-files/design-doc-template.md` exists. | The subtree indexes + skeleton design docs + the `template-files/` tier land via the normal file-merge on sync (restore via `grm-workflow-bootstrap --restore` if MISSING). Starter stubs — flesh out or replace; nothing to configure. No schema bump. | null |
| `skill-namespacing` | v3.42 | Every Grimoire skill is prefixed `grm-` (e.g. `grm-doc-assurance` → `grm-doc-assurance`) so the framework occupies a collision-proof namespace in a consumer's `.claude/skills/` — a consumer's own skills can never shadow or be shadowed by Grimoire's. All 63 skills across flavors + the `grm-workflow-bootstrap` golden tree renamed; every reference rewritten (paths aggressively, bare-name prose conservatively). Hard cutover, no aliases. Backed by a deterministic, reusable stdlib transformer (`grm-sync-from-upstream/grm_namespacing.py`) that is also the migrate engine below. No schema bump. | Check that `.claude/skills/` contains only `grm-`-prefixed skill dirs (no un-prefixed survivor) AND `.claude/skills/grm-doc-assurance/` exists. | The renamed dirs + rewritten references land via file-merge (restore via `grm-workflow-bootstrap --restore` if MISSING). | **Complete the bare-name → grm- cutover — NEVER auto-run.** After a sync, a pre-v3.42 project holds BOTH the new `grm-<name>/` (added by the file-walk) and the stale bare `<name>/`. Run `python3 .claude/skills/grm-sync-from-upstream/grm_namespacing.py --root . --apply`: where `grm-<name>/` is **absent** it `git mv`s `<name>`→`grm-<name>` (history-preserving); where `grm-<name>/` **already exists** (the post-sync case) it **archives** the stale `<name>/` to `.grimoire-archive/grm-namespacing-<ts>/` then removes it — the synced copy stays authoritative and it never nests `grm-<name>/<name>/`. References rewritten per the two-tier rule. Idempotent; `--dry-run` previews. A human/master decision since a consumer may reference skills by name in their own files. SKILL.md Step 4.55 surfaces this offer each sync via a manifest-independent survivor check. |
| `grm-end-session` | v3.42 | One-shot end-of-session orchestration skill: a guide/checklist that delegates (never reimplements) — merge remaining ready branches (`grm-release-phase-merge`), run `grm-project-release` if a version is staged (push + GitHub Release under the autonomous-push posture), post-release cleanup (prune merged branches/worktrees, clean `dist/`), then a session handoff (confirm tree clean + local==origin, update state memory, emit a next-session summary). Invoked via "end the session" / "wrap up" / "get ready for a fresh session". No schema bump. | Check that the `grm-end-session` skill dir exists (`.claude/skills/grm-end-session/SKILL.md`). | Passive skill lands via the normal file-merge (restore via `grm-workflow-bootstrap --restore` if MISSING). No config change, no schema bump. | null |
| `clean-room-separation` | v3.41 | Completes the v3.39 bulkhead for **operational** docs (Clean-Room): the remaining framework-internal operational docs (`integration-workflow.md`, `version-design.md`, `qa-ledger.md`, `release-planning-v*.md`, `token-efficiency-*.md`, `execution-profile-spike-s1.md`) relocate from top-level `docs/` to `docs/grimoire/` and join the exclusion set in **both** ship gates (`build_distributables.py` `EXCLUDED_PATH_PREFIXES` + `sync-from-upstream.sh` `is_excluded`); `version-history.md` becomes exclude-and-seed (consumer gets an empty template; Grimoire keeps its log); the active in-flight `release-planning-v{X.Y}.md` stays in place but is gate-excluded (tooling-path coupling). Adds a `shipped-pointers` `grm-doc-assurance` check enforcing the pointer-integrity invariant (no shipped doc relative-links an excluded/relocated target), sourcing the exclusion set from the build gate. No schema bump. | Check that top-level `docs/` contains no framework operational docs (e.g. no `docs/integration-workflow.md`, `docs/version-design.md`) — they live under `docs/grimoire/` — AND `doc_assurance.py shipped-pointers` passes. | Run `grm-sync-from-upstream`: the relocated layout + the two-gate exclusions + the `shipped-pointers` check land via file-merge, so a sync no longer delivers framework operational docs into top-level `docs/`. | **Archive-then-prune stranded framework operational docs — NEVER auto-run.** A project synced before v3.41 may have Grimoire operational docs left in top-level `docs/`; reconcile with `grm-docs-migrate` (archive under `.grimoire-archive/<ts>/` first, then remove) — a human/master decision since `docs/` also holds the project's own docs. |
| `grimoire-files-manifest` | v3.41 | Machine-readable authoritative enumeration of every Grimoire-owned file — the canonical delete-list distinct from project files (`.claude/grimoire-files.json`), tagging each path/glob `pure-framework` (safe delete+restore) / `mixed` (carries project content — merge, never blind-delete) / project-owned, with each entry's `regenerate_disposition`. Cross-references `manifest.md` (no second source of truth). Backed by a stdlib validator (`.claude/skills/grm-files-manifest/validate_files_manifest.py`) detecting UNLISTED_PRESENT / LISTED_ABSENT / MISTAG drift. Consumed by the `grm-regenerate-grimoire` partition. Per-flavor contents (claude-code/copilot/root); copilot lacks a golden baseline. No schema bump. | Check that `.claude/grimoire-files.json` exists AND `.claude/skills/grm-files-manifest/validate_files_manifest.py` exists. | Manifest + validator land via the normal file-merge (restore via `workflow-bootstrap --restore` if MISSING); run `validate_files_manifest.py --self-test` (and `--strict` against the tree) to verify. No schema bump. | null |
| `grm-regenerate-grimoire` | v3.41 | Surgical regenerate-from-scratch skill: deletes+restores ONLY the framework layer in place from the `grm-workflow-bootstrap` golden baseline (partitioned via `grimoire-files.json`), preserving project files; archive-then-restore for safety (`.grimoire-archive/<ts>/`, never silent loss); explicit mixed-file merge handling (`.claude/settings.json` 3-way, `CLAUDE.md`/`AGENTS.md` section+placeholder+sentinel preserve, `.gitignore` section-merge, `docs/roadmap.md` baseline-row reconcile, `docs/version-history.md` seed-or-leave); idempotent (second run = clean diff); `--check`/`--dry-run`. The middle path between `install-doctor --repair` and `grm-hard-reset`. Requires a golden baseline — copilot has none and the script refuses there (exit 2, documented). No schema bump. | Check that `.claude/skills/grm-regenerate-grimoire/` exists with `SKILL.md` + `regenerate_grimoire.py`. | Skill + script land via the normal file-merge (restore via `workflow-bootstrap --restore` if MISSING); run `regenerate_grimoire.py --self-test` (round-trip + idempotency + mixed-merge + rollback). Refuses without a golden baseline. No schema bump. | null |
| `autonomous-push-prompt-suppression` | v3.40 | Paradigm-aware suppression of the interactive `git push` permission prompt: `push-guard.sh` gains `should_auto_allow(paradigm, autopush)` and, after the full guard passes (marker + allowlist + denied destructive flags) and the approval is audit-logged, emits a `PreToolUse` `permissionDecision: allow` **iff** the project is Noir AND `autonomous-push.enabled` is true — so an opted-in unattended Noir release pushes without a human click, while Supervised/Weiss and Noir-without-the-opt-in stay human-gated. Adds an issue-tracker MCP permissions allowlist to `settings.json`. Claude-Code-only (Copilot push stays a human action via `git-hooks/pre-push`; no `push-guard.sh` there). No schema bump. | Run `push-guard.sh --self-test` — passes, including the `should_auto_allow` truth table ((Noir,true)→true; (Noir,false)/(Supervised,*)/(Weiss,true)→false). | **Security-sensitive — landing the code does NOT enable auto-push.** The `push-guard.sh` + `settings.json` edits land via the normal file-merge (restore via `workflow-bootstrap --restore` if MISSING); suppression activates ONLY when the project is Noir AND has explicitly set `autonomous-push: { enabled: true }`. Every guard rail (marker, allowlist, denied destructive flags) is unchanged. | null |
| `documentation-separation` | v3.39 | Framework-internal design docs separated from consumer-facing docs: Grimoire's `*-design.md` relocated to `docs/grimoire/design/` and excluded from **both** ship gates (`build_distributables.py` `EXCLUDED_PATH_PREFIXES` + `sync-from-upstream.sh` `is_excluded`), so they no longer land in a consumer's `docs/design/`; `docs/design/` is now consumer-project-own only; the wiki-convention authority moved to the shipped `docs/grimoire/README.md`. | Check that `docs/design/` contains no Grimoire framework `*-design.md` (e.g. no `doc-assurance-design.md` under `docs/design/`) — framework specs are internal and unshipped, never delivered into the consumer's `docs/design/`. | Run `grm-sync-from-upstream`: the relocated layout + the two-gate exclusion land via file-merge, so a sync no longer delivers framework design docs into `docs/design/`. | **Archive-then-prune stranded framework docs — NEVER auto-run.** A project synced before v3.39 may have Grimoire `*-design.md` left in its `docs/design/`; reconcile with `grm-docs-migrate` (archive under `.grimoire-archive/<ts>/` first, then remove), a human/master decision since `docs/design/` may also hold the project's own design docs. |
| `wiki-doc-hierarchy` | `v3.37` | `doc_assurance.py --self-test` passes; `check_lean_index` + `check_monolith_cap` run; `docs_migrate.py --self-test` passes from a downstream root | Run `doc_assurance.py --self-test` + `docs_migrate.py --self-test` | Run `grm-sync-from-upstream`; review the migration offer for docs; verify hierarchy checks pass | Full wiki-doc hierarchy: migration tool, corpus migration, bootstrap seeding, token-efficiency checks |
| `wiki-doc-migrate` | v3.37 | Migration mechanism: `docs_migrate.py --self-test` passes from a downstream project root with no `claude-code/`; produces conformant + idempotent tree; archives before rewrite | Run `docs_migrate.py --self-test`; dry-run on an old-style fixture shows OFFER path | Run `grm-sync-from-upstream` and confirm the migration offer; or run `docs_migrate.py --apply` directly | Notes: never auto-run under any paradigm |
| `wiki-doc-hierarchy-foundation` | v3.36 | Canonical docs root, tier index pages, and born-conformant doc producers: `claude-code/docs/README.md` (canonical flavor root with docs-map markers), tier indexes (`claude-code/docs/grimoire/README.md`, `docs/design/ux/README.md`), breadcrumb up-links in design + UX index pages. Three doc producers (`grm-design-doc-scaffold`, `grm-source-to-design-docs`, `grm-design-language-adapt`) updated to emit breadcrumbs + relative-link index rows by default. Convention authority in `docs/grimoire/docs-organization-design.md` §Wiki hierarchy & relative links (#93-#95). | Check `docs/README.md` exists AND `grm-doc-assurance` lists a `hierarchy` check. | Index pages and skill updates land via normal file-merge; no config change required. | null |
| `doc-assurance-hierarchy-enforcer` | v3.36 | Two new checks in `doc_assurance.py` (claude-code only): `check_hierarchy` (reachability from docs root, breadcrumb presence, tier index presence) and `check_relative_links` (absolute internal link rejection, broken anchor detection, bare-prose ref detection). Nested `build_map` writes between markers. `off/warn/block` dial reads from `grimoire-config.json` `doc-hierarchy.enforcer` key (#96). | Check `claude-code/.claude/skills/grm-doc-assurance/doc_assurance.py` contains `check_hierarchy`. | Script lands via normal file-merge (restore via `workflow-bootstrap --restore` if MISSING). Add `doc-hierarchy: {enforcer: {value: "warn"}}` to `grimoire-config.json` via `config-validate --migrate` (additive, no schema bump). Run `doc_assurance.py --self-test` to verify. | null |
| `doc-assurance-strict-gate` | v3.36 | `release-phase-merge/SKILL.md` §3b explicitly invokes `doc_assurance.py --strict` at release closeout: block-tier fails with auto-filed `doc-quality` findings; warn-tier (default) files and proceeds; Stealth Mode suppresses auto-filing. Replicated to all paradigm copies and golden trees (#97). | Check `.claude/skills/grm-release-phase-merge/SKILL.md` contains "Doc-assurance --strict gate" or "§3b". | Skill updates land via normal file-merge; no config change required. | null |
| `label-taxonomy-extensions` | v3.35 | Four new issue-label dimensions: size (XS/S/M/L/XL — token-band mapped, drives dispatch), complexity (simple/moderate/complex/research — tied to model tier), component (free-form per-project), and priority (very-low/low/normal/high/critical — replaces p0-p3 with backward compat) (#106-#109). Applied at triage; read by dispatch for model/effort sizing. | Check `.claude/skills/grm-issue-tracker/issue_tracker.py` contains `def ensure_label`. | Skill/doc updates land via normal file-merge; no config change required. | null |
| `ticket-layout-enforcement` | v3.35 | Enforces canonical three-section layout (Overview / Requirements / Acceptance Criteria) at filing time (`feedback-to-issue/SKILL.md`, `reporter/SKILL.md`) and validates at triage (`triager/SKILL.md` §0); missing sections trigger `needs-info` label and block triage (#110). | Check `.claude/skills/grm-triager/SKILL.md` contains a layout-validation step (§0 or "layout"). | Skill updates land via normal file-merge; no config change required. | null |
| `epic-support` | v3.35 | Adds Epic concept to `issue_tracker.py`: `issue_type` field (epic/issue), optional `parent_epic_id` field on child issues, auto-applied `epic` label, one-level nesting rule (Epics cannot be children of other Epics). `list_issues` filters by type. Triager §Epic creation covers the 3+ related issues threshold (#111). | Check `.claude/skills/grm-issue-tracker/issue_tracker.py` contains `issue_type` field (grep for `issue_type`). | Script lands via normal file-merge (restore via `workflow-bootstrap --restore` if MISSING). Run `issue_tracker.py --self-test` to verify. No config change. | null |
| `milestone-enforcement-gate` | v3.35 | Pre-dispatch hard gate in `release-phase/SKILL.md` (Step 3.5): asserts every planned work-item issue carries a `milestone:vX.Y` label for the current release; stops dispatch with a clear error listing unlabeled issues if any are missing. Triager gains §Milestone assignment (#112). | Check `.claude/skills/grm-release-phase/SKILL.md` contains "Step 3.5" or a "milestone" gate section. | Skill updates land via normal file-merge; no config change required. | null |
| `qa-close-gate` | v3.35 | Adversarial QA agent close gate: moves ticket closure from the implementing agent to a dedicated QA agent that reads Acceptance Criteria, greps the merged diff for each criterion, attempts to refute, and closes only on pass (flags with `needs-qa-fix` on fail). Integration master dispatches it post-merge, chip-free, non-blocking (#113). | Check `.claude/skills/grm-qa-agent/SKILL.md` contains "Issue close gate" or "adversarial" close section. | Skill updates land via normal file-merge; no config change required. | null |
| `runtime-verification-gate` | v3.34 | Runtime smoke verification gate — a new `smoke` verb in `recipe.py` (all four copies; `INTERFACE_VERSION` 3→4) that boots the app, curls the entry page and critical assets, asserts 2xx + correct content-type, and exits 0/nonzero. Mandatory for served/UI surfaces before `grm-project-release`. Integration: `integration-workflow.md` step "Runtime smoke check"; CLAUDE.md done-criteria updated; `build-recipe-interface-design.md` interface table updated. Design: `docs/grimoire/design/runtime-verification-design.md`. Mirrored across flavors (root, claude-code, copilot/scripts, both golden trees). | Check that `recipe.py` contains `def smoke(` or the `smoke` verb (grep `^\s*['\"]smoke['\"]` in `.claude/recipes.json`, or check `INTERFACE_VERSION` equals 4 in `recipe.py`). | The updated `recipe.py` (all four copies) lands via the normal file-merge (restore via `workflow-bootstrap --restore` if MISSING/DRIFTED). Verify with `recipe.py --self-test`. No config schema bump. | null |
| `changelog-surface` | v3.31 | Changelog Surface (CL-1..6) — required-feature catalog **Entry 2** (`catalog-version` 1→2): every Grimoire **web app** must surface its changelog through the GUI — **operator-facing always** (a Changelog section in the Admin Console) and **user-facing by toggle** (a public surface at the stable path `/changelog`, gated by the additive `changelog.user-facing` config dial: `{on,off}`, default `off`, absence-as-default, **no schema bump**). Data is a build-time snapshot carried by a new optional `grimoire-build-info.json` `changelog` field (`docs/web-app-deployment-protocol.md` §8), so it travels with the version like the config snapshot. `config_validate.py` adds `changelog` to `KNOWN_TOP` + `changelog.user-facing` to `ENUMS` (+ a non-object cross-rule + self-tests). Authority `docs/grimoire/design/changelog-surface-design.md`; registered in `docs/grimoire/design/web-app-support-design.md` §5.4. Mirrored across flavors (catalog: 4 Claude-flavor copies + the copilot `.github/prompts` copy carry Entry 2; `config_validate.py`: all 4 copies). SPEC only — the managed app implements the surfaces per the filed ticket. | Web-app projects only (`web-app.value: yes`): check the required-feature catalog is at `catalog-version: 2` — grep `^catalog-version: 2` in `.claude/skills/grm-web-app-apply/required-feature-catalog.md` (copilot: `.github/prompts/required-feature-catalog.md`). Non-web projects: not applicable. | Web-app projects: re-run the catalog filing flow (`grm-web-app-apply` §6 / onboarding §6.5) — a Reporter files the `[key: changelog-surface]` `Grimoire-Requirement` ticket (deduped by key; a re-run files nothing). The updated `config_validate.py` accepts the optional additive `changelog.user-facing` dial (default `off`; set `on` only to expose the user-facing `/changelog` surface). No schema bump. Verify with `config_validate.py --self-test`. Non-web projects: no action. | null |
| `dependency-channel` | v3.29 | Dependency Channel (DEP-CH-1..8) — a uniform, release-channel-sourced, **vendored** (committed) first-party dependency substrate. Producer: `build_distributables.py` emits a deterministic canonical `grimoire-v{X.Y}.tar.gz` + the generalized `release.json` (the sole manifest — `RELEASE-META.json` retired, converged into its superset), `SHA256SUMS` now covering `release.json`. Consumer: `.claude/skills/grm-sync-deps/` (`sync_deps.py` + `sync_deps_engine.py`, stdlib) resolves channel→version → downloads `release.json`/`SHA256SUMS`/artifact → **verifies sha256 before placement** → atomic-replaces `vendor/<dep>/` → writes JSON `vendor.lock` (two-hash: `artifact_sha256` wire + `tree_sha256` offline, both `sha256:<hex>`), with `--check`/`--offline`/`--update`. Two `recipe.py` verbs `grm-sync-deps` + `vendor-check` (`INTERFACE_VERSION` 3). Warn-only conformance gate `dependency-audit/dependency_channel_conformance.py` (three checks; recomputes a `tree_sha256` byte-identical to `grm-sync-deps`; wired into `grm-release-phase-merge` §3.5 via the live `code-quality.audit-gate` dial — no schema bump). `vendor-migrate/` (`vendor_migrate.py`) converts an existing submodule into `vendor.toml`+`vendor.lock` (loud no-match fallback). `grm-workflow-bootstrap` seeds a golden `vendor.toml` stub + empty `vendor.lock` + the two recipe verbs + the `.sync-deps-staging/` ignore (never clobbers). `grm-design-language-adapt` retargets web Aura consumption from git submodule to a `vendor.toml` dep synced via `grm-sync-deps` (new default; `dest = vendor/aura` invariant). Authority `docs/grimoire/design/dependency-channel-design.md`. Mirrored across flavors (claude-code + root skills/recipe/builder + both golden trees; copilot `scripts/sync_deps.py` + `sync_deps_engine.py` + `vendor_migrate.py` + `dependency_channel_conformance.py` + `recipe.py`). | Check that `vendor.toml` exists AND `.claude/recipes.json` contains `grm-sync-deps`. | The `grm-sync-deps`/`grm-vendor-migrate` skills + the conformance gate + the channel-aware builder + the `vendor.toml`/`vendor.lock` seeds land via the normal file-merge (restore via `workflow-bootstrap --restore` if MISSING/DRIFTED). **Additive, advisory this release, no schema bump** — `grm-workflow-bootstrap` seeds `vendor.toml`/`vendor.lock`/the two recipe verbs/the staging ignore idempotently (never clobbers a customized `vendor.toml`); declare deps with `[deps.<name>]` blocks and run `recipe.py sync-deps`; `vendor-check` is warn-only. Verify with `sync_deps.py --self-test`, `vendor_migrate.py --self-test`, `dependency_channel_conformance.py --self-test`, and `recipe.py --self-test`. No config-schema bump. | null |
| `grimoire-status-server` | v3.28 | `grimoire-status` MCP server (OPS-1, audit rank 2 — the "second instance from the template" `mcp-server-design.md` follow-up). A thin read-only `McpServer` subclass (`.claude/mcp-servers/grimoire-status/server.py`, over `mcp_runtime.py`) wrapping `status-broker/project_status.py`; one tool `get_status` returns the structured JSON project overview (name, framework version, paradigm, dials, latest/in-flight release, manifest version, tech stack, degraded-source warnings). **Read-only** — no writes, no git, no tracker calls. Registered via the root `.mcp.json` (`grimoire-status`); `status-broker/SKILL.md` re-pointed MCP-first with CLI fallback. Authority `docs/grimoire/design/status-broker-design.md` + `mcp-server-design.md`. Layout-agnostic across flavors (claude-code `.claude/mcp-servers/`; copilot `mcp-servers/`). | Check that `.mcp.json` contains `grimoire-status` AND `.claude/mcp-servers/grimoire-status/server.py` exists. | Server + re-pointed `grm-status-broker` skill land via the normal file-merge (restore via `workflow-bootstrap --restore` if MISSING/DRIFTED); **merge** the `grimoire-status` entry into any existing `.mcp.json` (read-merge-write — never clobber other `mcpServers`). The MCP-first re-point is **graceful**: absent/disabled MCP, the skill falls back to `project_status.py` CLI unchanged. Verify with `server.py --self-test`. No schema bump. | null |
| `grimoire-recipe-server` | v3.28 | `grimoire-recipe` MCP server (OPS-2, audit rank 5). A thin `McpServer` subclass (`.claude/mcp-servers/grimoire-recipe/server.py`) over `build-recipe/recipe.py`; three tools `list_targets`/`dry_run`/`run_recipe` (`run_recipe` returns structured `{target, exit_code, ok, stdout, stderr}` rather than free-form subprocess output). Recipes remain project-defined in `.claude/recipes.json` — the server adds **no new execution authority**. Registered via the root `.mcp.json` (`grimoire-recipe`); `build-recipe/SKILL.md` re-pointed MCP-first with CLI fallback. Authority `docs/grimoire/design/build-recipe-interface-design.md` + `mcp-server-design.md`. Layout-agnostic across flavors. | Check that `.mcp.json` contains `grimoire-recipe` AND `.claude/mcp-servers/grimoire-recipe/server.py` exists. | Server + re-pointed `grm-build-recipe` skill land via the normal file-merge (restore via `workflow-bootstrap --restore` if MISSING/DRIFTED); **merge** the `grimoire-recipe` entry into any existing `.mcp.json` (read-merge-write). Graceful re-point: absent/disabled MCP, the skill falls back to `recipe.py` CLI unchanged. Verify with `server.py --self-test`. No schema bump. | null |
| `grimoire-environment-server` | v3.28 | `grimoire-environment` read-only MCP tools (OPS-3, audit rank 8). A thin `McpServer` subclass (`.claude/mcp-servers/grimoire-environment/server.py`) over `environment-manager/env_probe.py`; three **read-only** tools `list_processes`/`port_status`/`instance_urls`. Lifecycle operations (`kill`/`start`) are deliberately **excluded** — per `environment-manager-design.md` §3 they stay per-action-authorized agent-side. Registered via the root `.mcp.json` (`grimoire-environment`); `environment-manager/SKILL.md` re-pointed MCP-first for the read paths only. Authority `docs/grimoire/design/environment-manager-design.md` + `mcp-server-design.md`. Layout-agnostic across flavors. | Check that `.mcp.json` contains `grimoire-environment` AND `.claude/mcp-servers/grimoire-environment/server.py` exists. | Server + re-pointed `grm-environment-manager` skill land via the normal file-merge (restore via `workflow-bootstrap --restore` if MISSING/DRIFTED); **merge** the `grimoire-environment` entry into any existing `.mcp.json` (read-merge-write). Graceful re-point: absent/disabled MCP, the skill falls back to `env_probe.py` CLI unchanged; lifecycle stays agent-side either way. Verify with `server.py --self-test`. No schema bump. | null |
| `cost-budget-script` | v3.28 | `cost_budget.py` deterministic budget engine + transcript-locate (GOV-1, audit ranks 4+10). A stdlib `cost_budget.py` under `.claude/skills/grm-cost-budget/` implements window rolling, once-per-window threshold-crossing detection, and `cost-utilization.json` ledger arithmetic deterministically, reusing `grm-token-measure`'s `parse_usage.py` for transcript parsing; `parse_usage.py` gains `--locate-transcript` (resolves `~/.claude/projects/<encoded-cwd>/…` deterministically — no agent-side path encoding). `cost-budget/SKILL.md` + `token-measure/SKILL.md` re-pointed script-first. No cost MCP server in this release. Authority `docs/grimoire/design/cost-governance-design.md` + `mcp-expansion-audit.md` ranks 4+10. Mirrored across flavors (claude-code + root scripts; copilot `scripts/cost_budget.py` + `parse_usage.py`). | Check that `.claude/skills/grm-cost-budget/cost_budget.py` exists AND `.claude/skills/grm-token-measure/parse_usage.py` contains `--locate-transcript` (or `locate-transcript`). | The `cost_budget.py` engine + the updated `parse_usage.py` + the re-pointed SKILL.mds land via the normal file-merge (restore via `workflow-bootstrap --restore` if MISSING/DRIFTED). The re-point is mechanical (script-first); the engine is `--self-test`-backed. Verify with `cost_budget.py --self-test` and `parse_usage.py --self-test`. No config change required. No schema bump. | null |
| `component-registry-script` | v3.28 | `component_registry.py` deterministic registry engine (REG-1, audit rank 3). A stdlib `component_registry.py` under `.claude/skills/grm-component-registry/` with byte-identical idempotent JSON output, sha256 content hashing, content-derived build id, sorted-key serialization, atomic temp+replace write, and taxonomy validation (unknown tags surfaced, never dropped) — a script, not an MCP server (batch op run infrequently). Both consumer skills (`grm-component-registry`, `grm-component-catalog-export`) re-pointed script-first; the export remains a read-only **view** (`dry-run --stdout`, never mutates the registry). Authority `docs/grimoire/design/scripting-unification-design.md` §5 + `mcp-expansion-audit.md` rank 3. Mirrored across flavors (claude-code + root script; copilot `scripts/component_registry.py`). | Check that `.claude/skills/grm-component-registry/component_registry.py` exists. | The `component_registry.py` engine + the two re-pointed consumer skills land via the normal file-merge (restore via `workflow-bootstrap --restore` if MISSING/DRIFTED). The script is **file-write-only** (writes only `.claude/component-registry.json`; the agent commits) and idempotent (a second run on unchanged sources is byte-identical). Verify with `component_registry.py --self-test`. No config change required. No schema bump. | null |
| `grimoire-release-server` | v3.27 | `grimoire-release` MCP server (MCP-1) + the stdlib `release_plan.py` ledger engine it adapts. The engine (`.claude/skills/grm-release-agent-tracker/release_plan.py`) locates the active plan, parses the §5 ledger to JSON, diffs ledger-vs-`git branch`, computes the merge queue from §3's conflict map, ticks rows atomically/idempotently, runs merge preflight (HEAD-verification + isolation/commits-ahead asserts + `mergeAfter` toposort), and emits phase batch manifests. The server (`.claude/mcp-servers/grimoire-release/server.py`, a second `McpServer` instance over `mcp_runtime.py`) exposes `get_ledger`/`tick_rows`/`merge_queue`/`merge_preflight`/`plan_phase`/`read_loop_state`/`advance_loop` — **file-write-only** (never runs git mutations; the agent commits). Registered via the root `.mcp.json` (`grimoire-release`). Consumers re-pointed MCP-first with CLI fallback: `grm-release-agent-tracker`, `grm-ledger-tick`, `grm-release-phase`, `grm-release-phase-merge`, `grm-noir-loop`. Authority `docs/grimoire/design/grimoire-release-server-design.md`. Layout-agnostic across flavors (claude-code `.claude/mcp-servers/` + `.claude/skills/`; copilot `mcp-servers/` + `scripts/`). | Check that `.mcp.json` contains `grimoire-release` AND `.claude/mcp-servers/grimoire-release/server.py` exists AND `.claude/skills/grm-release-agent-tracker/release_plan.py` exists. | Engine + server + re-pointed consumer skills land via the normal file-merge (restore via `workflow-bootstrap --restore` if MISSING/DRIFTED); **merge** the `grimoire-release` entry into any existing `.mcp.json` (read-merge-write — never clobber other `mcpServers`). The MCP-first re-point is **graceful**: absent/disabled MCP, consumers fall back to `release_plan.py` CLI unchanged. Verify with `release_plan.py --self-test` and `server.py --self-test`. No schema bump. | null |
| `release-phase-merge-split` | v3.27 | `grm-release-phase-merge` SKILL.md split under the 12 KB context budget (MCP-2, #85). With MCP-1's preflight extraction removing the bulk of the deterministic prose, the 16 KB SKILL.md is split into a lean active head + a `reference.md` companion (≤ 12 000 bytes), no behavioral change to the merge protocol; covers the Noir/Supervised/Weiss paradigm twins. The root↔claude-code divergence is paradigm-legitimate (root = Noir-installed, claude-code = Supervised default) and documented in `reference.md` §Divergence note. Clears the skill-budget `grm-doc-assurance` finding for this skill. Mirrored across flavors (claude-code + root skill + paradigm twins; copilot folds the reference content into the single `release-phase-merge.prompt.md`). | Check that `.claude/skills/grm-release-phase-merge/reference.md` exists AND `.claude/skills/grm-release-phase-merge/SKILL.md` is ≤ 12000 bytes. | The split SKILL.md + new `reference.md` land via the normal file-merge (restore via `workflow-bootstrap --restore` if MISSING/DRIFTED). Mechanical doc change — no config dial, no behavior change to the merge protocol. Run `grm-doc-assurance skill-budget` to confirm the finding clears. No schema bump. | null |
| `deploy-environment-model` | v3.27 | Deploy-environment model + `scripts/deploy.sh` interface (DEP-1, #88+#90). An additive `environments` block in `grimoire-config.json` registered in `config_validate.py` (per-env `data_isolation`/`channel`/`deploy_policy`/service-address fields), the `APP_ENV` convention, named environments `local`/`dev`/`beta`/`production`, and the standard `scripts/deploy.sh <env> [<version>]` interface documented in `docs/web-app-deployment-protocol.md` §Environments with the build-recipe `deploy` target wired to it. Does NOT extend the v3.4 grm-environment-manager skill. Authority `docs/grimoire/design/deploy-environment-design.md` + the protocol doc §Environments. Mirrored across flavors (claude-code + root `config_validate.py`/`grm-build-recipe`/protocol; copilot `build-recipe.prompt.md` — `grm-config-validate` is Claude-Code-only). | Check that `config_validate.py` registers `environments` (the skill file `.claude/skills/grm-config-validate/config_validate.py` contains `environments`). | The updated `config_validate.py` + `grm-build-recipe` deploy wiring + protocol doc land via the normal file-merge (restore via `workflow-bootstrap --restore` if MISSING/DRIFTED). The `environments` block is **opt-in and additive** (absent ⇒ no environments registered); a web project declares its environments and wires `recipes.json` `deploy` to `scripts/deploy.sh`. Run `config_validate.py --self-test` to verify the block. No schema-version bump. | null |
| `release-channels-signing` | v3.27 | Release channels + signed assets + meta-updater (DEP-2, #87). Stable/beta release **channels** in the distribution contract (channel-aware `build_distributables.py --channel` + release naming with a `-beta` suffix + `RELEASE-META.json` `channel`), **signed assets** (`SHA256SUMS` always; `SHA256SUMS.minisig` minisign signature when `minisign`+`MINISIGN_SECRET_KEY` are present — else a loud unsigned-build warning, never a silent skip), and a `grm-sync-from-upstream` `UPSTREAM_CHANNEL=stable|beta` selector with transport-level SHA256SUMS verification of downloaded release assets (match ⇒ proceed; mismatch ⇒ fall back to git; absent ⇒ loud degradation). Extends `release-distribution-design.md` + `grm-project-release` §GitHub Release. Mirrored across flavors (sync transport + builder reference in claude-code + copilot `scripts/`; builder is the upstream maintainer tool). | Check that `.claude/skills/grm-project-release/build_distributables.py` contains `--channel` (or `SHA256SUMS`) AND `.claude/skills/grm-sync-from-upstream/sync-from-upstream.sh` contains `UPSTREAM_CHANNEL`. | The channel-aware builder + the `UPSTREAM_CHANNEL`/SHA256SUMS sync transport land via the normal file-merge (restore via `workflow-bootstrap --restore` if MISSING/DRIFTED). **Backward-compatible/opt-in** — default channel is `stable` and signing degrades loudly when `minisign` is absent, so existing flows are byte-compatible; pin a channel with `UPSTREAM_CHANNEL=beta` and sign by exporting `MINISIGN_SECRET_KEY`. Run `build_distributables.py --self-test` and `sync-from-upstream.sh --self-test` to verify. No config-schema bump. | null |
| `fleet-status-contract` | v3.27 | Fleet Status Contract v1 canonical spec + conformance (DEP-3, #89). Extracts the contract from Familiar's shipped `fleet.rs` into a canonical spec (`docs/grimoire/design/fleet-status-contract.md`): `GET /fleet/v1/status` full/minimal shapes (optional-auth, never-401), `fleet-instance.json` deploy-time manifest, schema-version rules (absent ≡ null for `update.available`/`last_checked`). Adds the `fleet_conformance.py` conformance checker (offline fixture `--self-test` or live `--url`) and the `web-app-fleet-status` `baseline-requirements.md` row at baseline-version 3 so onboarding/retrofit seed it. Authority `docs/grimoire/design/fleet-status-contract.md`. Mirrored across flavors (claude-code + root `fleet_conformance.py` + baseline; copilot `scripts/fleet_conformance.py` + baseline companion). | Check that `fleet_conformance.py` exists in the `grm-web-app-apply` skill dir (`.claude/skills/grm-web-app-apply/fleet_conformance.py`). | The spec doc + `fleet_conformance.py` + the baseline-requirements row (baseline-version 3) land via the normal file-merge (restore via `workflow-bootstrap --restore` if MISSING/DRIFTED). The `web-app-fleet-status` obligation seeds for web projects via the `grm-web-app-apply` retrofit / onboarding baseline seeding; validate a deployed instance offline with `fleet_conformance.py --self-test` or live with `--url`. No config change required. No schema bump. | null |
| `doc-assurance-house-template` | v3.27 | `grm-doc-assurance` design-doc patterns reconciled with the house template (QOL-1). `doc_assurance.py`'s design-doc section checker — which previously required only the legacy `goals`/`non-goal`/`validation|idempotency` set — now also accepts the design-doc-scaffold house template (`Motivation`/`Scope`/`Design`-or-`Acceptance`), so a doc satisfying *either* pattern set passes. Clears the design-doc-layout noise class (was 55+ findings, ~3 added per new house-template doc) without weakening the check. Claude-Code-only (the `grm-doc-assurance` skill is not mirrored to copilot). | Check that `.claude/skills/grm-doc-assurance/doc_assurance.py` accepts the house template (the script contains `scope` and `acceptance` in its design-layout pattern set). | The updated `doc_assurance.py` lands via the normal file-merge (restore via `workflow-bootstrap --restore` if DRIFTED). Behavior-active once the file lands — re-run `grm-doc-assurance` to see the design-layout finding count drop. No config change required. No schema bump. | null |
| `web-app-config-detection` | v3.26 | Web-app config block + onboarding/bootstrap detection ("Web-app support" flagship). An additive `web-app` block in `.claude/grimoire-config.json` — `{ "value": "yes"\|"no", "stack": <hint>\|null }` — keyed on the narrower fact *is this a browser-delivered, server-hosted web app?* (a strict subset of GUI). Registered in `config_validate.py` (KNOWN_TOP + ENUMS) with **no** schema bump — absence ≡ not-a-web-app. Onboarding Step 4 and the `SKIP ONBOARDING` §2 inference distinguish web-app from generic GUI; `grm-workflow-bootstrap` Step 3 Q9's 20-row signal table persists the **confirmed** answer (detection pre-fills the default, never auto-commits); `grm-quick-start-template` Step 1's web-profile read becomes a real config read. Authority `docs/grimoire/design/web-app-support-design.md`. Mirrored across flavors (claude-code + root active skills; copilot onboarding/quick-start prompts; `grm-config-validate` is a Claude-Code-only skill). | Check `config_validate.py` registers `web-app` (the skill file `.claude/skills/grm-config-validate/config_validate.py` contains `web-app`) AND `docs/grimoire/design/web-app-support-design.md` exists. | The updated `config_validate.py` + onboarding/bootstrap/quick-start skills land via the normal file-merge (restore via `workflow-bootstrap --restore` if MISSING/DRIFTED). The `web-app` block is **opt-in and additive** (absent ⇒ not-a-web-app) — set it on a web project via the `grm-web-app-apply` retrofit skill, or it is captured at the next onboarding/bootstrap detection. No schema-version bump. Run `config_validate.py --self-test` to verify. | null |
| `grm-web-app-apply` | v3.26 | `grm-web-app-apply` retrofit skill for already-bootstrapped projects. Built on the canonical switch-skill pattern (fail-closed validation, idempotent early-exit, pure-data config write, error-conditions table) plus a detection front-end: re-run the Q9 signal table read-only against the existing repo, surface evidence, confirm (auto-pick under Noir), write the `web-app` config block, and seed the web-app obligations (baseline-requirement rows, deployment-protocol pointer, recipe deploy/package stubs) and catalog filing. Idempotent — a second run on the same project is a no-op; running on a non-web project exits cleanly without writes. Restores with its companions `reference.md` (Q9 signal table) and `required-feature-catalog.md`. Authority `docs/grimoire/design/web-app-support-design.md` §3. Mirrored across flavors (claude-code + root skill; copilot `web-app-apply.prompt.md` + catalog companion). | Check that the `grm-web-app-apply` skill dir exists (`.claude/skills/grm-web-app-apply/` with `SKILL.md`, `reference.md`, `required-feature-catalog.md`). | Passive skill + companions land via the normal file-merge (restore via `workflow-bootstrap --restore` if MISSING). Invoke `grm-web-app-apply` on demand to retrofit web-app support onto a project bootstrapped before v3.26; it fails closed on a missing config or a non-web repo. After a run, `grm-install-doctor` sees the feature as adopted. No config change required to install the skill. No schema bump. | null |
| `web-app-deployment-protocol` | v3.26 | Web-app deployment-flow protocol (`docs/web-app-deployment-protocol.md`) generalized from Familiar's shipped flow: versioned tar.gz bundle + `release.json` manifest, `versions/` + atomically-flipped `current` symlink, supervisor service verbs, health-gated self-update with auto-rollback, a `/healthz` contract, update channels, and a build-info stamping contract (`grimoire-build-info.json` = framework version + grimoire-config snapshot at build time). Companion mechanics: build-recipe **INTERFACE_VERSION 2** adds `package`/`deploy` target stubs (versioned, extend-only under sync, loud exit-2 when unimplemented); `baseline-requirements.md` gains web-app obligation rows (healthz, deploy bundle, service supervision) at baseline-version 2. Authority `docs/web-app-deployment-protocol.md` + `docs/grimoire/design/web-app-support-design.md` §4. Mirrored across flavors (claude-code + root `recipe.py`/baseline; copilot `build-recipe.prompt.md` + `recipe.py` + baseline companion). | Check that `docs/web-app-deployment-protocol.md` exists AND `recipe.py` is at interface v2 (`.claude/skills/grm-build-recipe/recipe.py` contains `INTERFACE_VERSION = 2` or the `package`/`deploy` targets). | The protocol doc + the updated `recipe.py` (interface v2) + the baseline-requirements rows land via the normal file-merge (restore via `workflow-bootstrap --restore` if MISSING/DRIFTED). The `package`/`deploy` targets arrive as **extend-only stubs** — `grm-sync-from-upstream` adds them without overwriting any implemented target; a project implements them per the protocol when it ships a web app. Run `recipe.py --self-test` to verify the interface version. No config change required. No schema bump. | null |
| `grimoire-requirement-mechanics` | v3.26 | `Grimoire-Requirement` protected-label mechanics + always-prioritized planning hooks. Adds an `ensure_label` operation per provider to the issue-tracker abstraction (`gh label create` for github, no-op for roadmap, not_implemented for grimoire), exposed through the engine and the bundled MCP server — `create()`/`label()` auto-ensure requested labels so a tagged draft never hits a rejected-unknown-label error on GitHub. `Grimoire-Requirement` is defined as a **protected label**: `grm-feedback-to-issue`'s vocabulary admits it (internal-only), the Triager must never remove/stale-close/downgrade tagged issues, and `grm-release-planning` Step 2/3 promote the tracker read into a **mandatory origin-D** (never-optional context) with `grm-release-agreement` carrying a never-silently-trimmed rule. Authority `docs/grimoire/design/issue-label-taxonomy.md` + `docs/grimoire/design/issue-tracker-design.md`. Mirrored across flavors (claude-code + root `issue_tracker.py`/MCP server/skills; copilot `scripts/issue_tracker.py` + MCP server + prompts). | Check that `issue_tracker.py` implements `ensure_label` (the skill file `.claude/skills/grm-issue-tracker/issue_tracker.py` contains `def ensure_label`). | The updated `issue_tracker.py` + MCP server + `grm-feedback-to-issue`/`grm-triager`/`grm-release-planning`/`grm-release-agreement` skills land via the normal file-merge (restore via `workflow-bootstrap --restore` if MISSING/DRIFTED). The label mechanics are **behaviour-active** once the files land — no config dial; `ensure_label` is invoked automatically inside create/label flows, and the planning/agreement carve-outs apply on the next planning cycle. Run `issue_tracker.py --self-test` to verify `ensure_label`. No schema bump. | null |
| `release-distribution` | v3.23 | GitHub Releases as the authoritative dependency. Every Grimoire release now **always** publishes a GitHub Release that carries the curated `version-history` notes and **per-flavor `.zip` distributables** — `grimoire-<flavor>-v{X.Y}.zip`, one per flavor (claude-code, copilot, and any future flavor), so downstream consumers can "look to Releases to know what to sync" and pull the distributable from the Release. A flavor is any top-level dir carrying a `.grimoire-flavor` marker; the stdlib helper `project-release/build_distributables.py` (deterministic, `--self-test`) auto-discovers them and builds reproducible zips containing the flavor tree (with its top-level name, so unzip yields `<flavor>/…` — the shape sync already consumes) plus that flavor's `feature-manifest.md`. `grm-sync-from-upstream` gains a `UPSTREAM_TRANSPORT` selector (`auto`/`local`/`release`/`git`, default `auto`): when `gh` is present and `UPSTREAM_REF` is a version tag it downloads + unzips `grimoire-<flavor>-*.zip` via `gh release download`, otherwise it falls back to the historical shallow git clone (and local-path) — **additive, no regression**. The `feature-manifest` stays the structured source of truth for *what* to adopt; Releases transport it, they do not replace it. Mirrored across flavors (sync transport in claude-code + copilot `scripts/`; builder is the upstream Claude-Code maintainer tool). | Check that `.claude/skills/grm-sync-from-upstream/sync-from-upstream.sh` contains `UPSTREAM_TRANSPORT`. | The updated `sync-from-upstream.sh` (with the release transport) lands via the normal file-merge (restore via `workflow-bootstrap --restore` if MISSING/DRIFTED). The transport is **opt-in/auto and backward-compatible** — absent `gh` or a non-tag `UPSTREAM_REF`, behaviour is byte-for-byte the old git-clone path, so no action is required to keep working; to consume release assets, set `UPSTREAM_REF` to a version tag (the script auto-selects the release transport when `gh` is available, or force it with `UPSTREAM_TRANSPORT=release`). Run `sync-from-upstream.sh --self-test` to verify the selector. No config-schema bump. | null |
| `agent-teardown` | v3.22 | End-of-run teardown for the high-level Noir agents (integration master, project manager, release-master `/loop`): a mandatory final **§Run teardown (end-of-run)** step — run after §Post-release cleanup when the run finishes (milestone reached, or stop with no work outstanding) — that closes three leaks left by per-run autonomy. (1) **Cancel self-created resume schedules** (`CronList`→`CronDelete`; do not re-arm `ScheduleWakeup`), the de-scheduling counterpart to default-on resume scheduling (#13), so a finished campaign leaves no live timer waking to nothing. (2) **Hand off the agent's own worktree** — the marker-blessed master cannot `git worktree remove` the worktree it runs in (busy checkout / held HEAD), so it surfaces the path + exact removal command for the operator or parent PM instead of silently abandoning it (the gap that left `crazy-dirac-2d4ac4` behind after the v3.16–v3.21 program). (3) Drop the now-stale `.claude/integration-allow.local` marker and clear scratch (`/tmp/notes-*.md`). The full procedure lives in `integration-workflow.md` §Run teardown (a budget-exempt doc); the IM/PM/noir-loop skills carry only a lean pointer, so the tight `grm-project-manager` skill stays under the 12 KB budget (no new skill-budget finding). No config dial, no new hook, no schema bump — agent-behavior guidance backed by existing tools (`CronList`/`CronDelete`, `git worktree`). Mirrored across flavors (claude-code paradigm + root active skills + copilot prompts; Copilot has no scheduling primitive, so only the worktree/marker/scratch steps apply there). | Check `.claude/skills/grm-integration-master/reference.md` contains `## Run teardown`. | Passive doc + skill-pointer content lands via the normal file-merge (restore via `workflow-bootstrap --restore` if MISSING). Teardown is **active under Noir** (the §Run teardown step lives in the Noir paradigm files and the IM/PM/grm-noir-loop skill pointers); switching a project to Noir via `grm-work-paradigm-switch` installs it. No config change required; no schema bump. | null |
| `grm-architecture-audit` | v3.18 | Deterministic architecture-fitness skill (v3.16–v3.21 code-quality program): `grm-architecture-audit` reads an opt-in `.claude/architecture-rules.json` (layers / allowed-edges / forbidden-imports / forbid-cycles) and checks the project's import graph against it — turning the narrative `architecture-guidelines.md` into machine-checked fitness functions (dependency-direction, public-surface, module-instability/size, cycle detection). Ships with `.claude/architecture-rules.example.json` (a valid, documented starter template) and pairs with the program's expanded audit-hints (`arch-dependency-direction`, `arch-public-surface`, `arch-module-instability`, `arch-module-size`). Mirrored across flavors (claude-code skill + copilot `architecture-audit.prompt.md`). | Check that the `grm-architecture-audit` skill dir exists (`.claude/skills/grm-architecture-audit/`) AND `.claude/architecture-rules.example.json` exists. | Passive skill + example template land via the normal file-merge (restore via `workflow-bootstrap --restore` if MISSING). The fitness checks are **opt-in**: copy `.claude/architecture-rules.example.json` to `.claude/architecture-rules.json` and edit it to your layering; absent the rules file the skill reports nothing to enforce (narrative guidelines still apply). Invoked on demand; no config change required. No schema bump. | null |
| `noir-iterative-loop` | v3.13 | Noir iterative release loop (#83): under Noir, each Claude Code `/loop` firing spawns ONE `release-master` subagent that owns a whole release iteration (plan→distribute→integrate→release, wrapping the integration-master pipeline) in isolated context and returns only a 1–2 sentence summary; cross-iteration continuity lives in the **size-budgeted** `.claude/cache/noir-loop-state.json` (gitignored), read/written by the stdlib-only `noir_loop_state.py` helper (refuses over-budget writes rather than truncating, so each spawned subagent reads it near-clean). New `release-master` role in the registry (`docs/grimoire/design/agent-roles-design.md` §B.12, name resolved against taxonomy #20); `/loop` works without `/clear`/`/compact`, composing with default Noir wakeup-scheduling (#13) and the token budget (#28). The loop **orchestration** is Claude-Code-only; the `noir_loop_state.py` helper is mirrored to copilot `scripts/`. | Check that the `grm-noir-loop` skill dir exists (`.claude/skills/grm-noir-loop/` with `SKILL.md` + `noir_loop_state.py`). | Passive skill + script land via the normal file-merge (restore via `workflow-bootstrap --restore` if MISSING). Gitignore covers `.claude/cache/` (where `noir-loop-state.json` lives) already. Run `noir_loop_state.py --self-test` to verify. No config change required (the loop is driven on demand under Noir via `/loop`). No schema bump. | null |
| `run-metadata-artifact` | v3.14 | Per-run metadata telemetry artifact (#82): a stdlib-only `run_metadata.py` writer that emits one structured JSON file per run under `.claude/cache/runs/<run_id>.json` (gitignored) in the shape Mission Control's Telemetry (Pulse) pillar documented, so `GrimoireRunSource` has an accurate, idempotent structured source instead of file-mtime guesses. Required fields `run_id`/`paradigm`/`profile`/`model`/`release`/`tokens.{input,output,cache_read,cache_creation}`/`items`/`items_passed`/`outcome∈{pass,fail,partial}`/`wall_clock_secs`/`started_at`; `run_id` is a stable dedup key (composed `v{release}-{epoch}` id, or content hash fallback) for idempotent re-ingest. The token split is **reused** from `grm-token-measure`'s `parse_usage.py` (not re-derived), degrading to all-zero tokens when no transcript is available. Emission is wired **gracefully** into `grm-release-phase-merge` (post `version/{X.Y}`→`dev`) and `grm-project-release` (post-tag) — absent inputs degrade, a non-zero exit is swallowed, telemetry never gates or blocks a release. Write-only dev telemetry, no push pipeline. The helper is mirrored to copilot `scripts/run_metadata.py` (with `parse_usage.py`). | Check that `run_metadata.py` exists in the `grm-token-measure` skill dir (`.claude/skills/grm-token-measure/run_metadata.py`). | Passive script lands via the normal file-merge (restore via `workflow-bootstrap --restore` if MISSING). Gitignore covers `.claude/cache/` (where `runs/*.json` lives) already. The two release skills consult the helper best-effort — absent the helper, emission is skipped, never an error. Run `run_metadata.py --self-test` to verify. No config change required. No schema bump. | null |
| `git-protocol-guard-hardening` | v3.15 | Git-protocol governance guard hardening (#84): `protected-branch-guard.sh` gains a fail-closed **history-rewrite deny rule** that BLOCKS `git rebase`, `git cherry-pick`, and `git reset --hard` on a protected branch (`dev`/`main`/`version/*`) for **every** actor (the marker-blessed master included) — stricter than the existing (actor, branch-class) commit/merge model, which is left untouched. Escape hatches (`--abort`/`--quit`/`--skip`) and soft/mixed resets stay allowed; force-push / force flags / remote-ref deletion remain owned by `push-guard.sh`. Branch-and-merge is documented as the git default and history-rewriting commands as last-resort across `CLAUDE.md`, `docs/grimoire/integration-workflow.md` (all paradigms+flavors), `grm-worktree-preflight`, and `grm-release-agreement` (whose protected-branch `cherry-pick` is replaced with `merge --no-ff`). Copilot ships a native `pre-rebase` hook (cherry-pick/reset --hard stay advisory there — git fires no hook). In-file `--self-test` on the guard. | Check that `.claude/hooks/protected-branch-guard.sh` contains `REWRITE_OPS` (the v3.15 history-rewrite deny rule). | The updated guard lands via the normal file-merge (restore via `workflow-bootstrap --restore` if MISSING/DRIFTED); it is byte-identical across root/claude-code/golden. Copilot adopters add the `pre-rebase` hook and re-run `git-hooks/install.sh`. Run `protected-branch-guard.sh --self-test` to verify. No config change required. No schema bump. | null |
| `quickstart-idempotent-db-up` | v3.15 | Idempotent multi-worktree `db-up` quick-start recipe (#81): the dockerized-service and web quick-start templates ship a `justfile` + `compose.yaml` whose `db-up` recipe is idempotent across git worktrees — it reuses an already-running shared DB container by **exact name** (`docker ps -f name=^/<name>$ -f health=healthy`), `docker start`s a stopped one, and only `docker compose up`s when none exists. It uses `docker ps` **filters** (NOT Go-template `--format '{{...}}'`, which collides with `just`'s `{{ }}` interpolation and breaks the recipe). The multi-worktree shared-container behaviour is documented inline near the DB recipes (all worktrees share one container on `localhost:5432`; recreating under another compose project attaches a different empty volume and loses seeded data). | Check that `.claude/quick-start-templates/service/files/justfile` exists AND contains `db-up`. | Passive template payload lands via the normal file-merge (restore via `workflow-bootstrap --restore` if MISSING). Apply on demand via `grm-quick-start-template` (service/web profiles). A project with an older justfile re-creates the `db-up` recipe from the template form. No config change required. No schema bump. | null |
| `grm-component-catalog-export` | v1.21 | Skill that scans reusable components (`component.json`/front-matter) and emits a machine + human-readable catalog report. | Check that the `grm-component-catalog-export` skill dir exists (`.claude/skills/grm-component-catalog-export/`). | Passive skill file — lands via the normal file-merge; if MISSING after merge, run `workflow-bootstrap --restore`. No config change required (invoked on demand). | null |
| `grm-quick-start-template` | v1.21 | Skill that matches the declared app profile against `.claude/quick-start-templates/*` and applies the closest template (scaffold + config defaults). | Check that the `grm-quick-start-template` skill dir exists (`.claude/skills/grm-quick-start-template/`). | Passive skill file — lands via the normal file-merge; if MISSING after merge, run `workflow-bootstrap --restore`. Optionally seed `.claude/quick-start-templates/` with project templates. | null |
| `grm-coding-practices-audit` | v1.20 | Agent-driven adherence audit of code against the audit-hints in `coding-standards.md`/`architecture-guidelines.md`/sub-docs; reports gaps and (with `--file-issues`) files one issue per gap via `grm-feedback-to-issue`. | Check that the `grm-coding-practices-audit` skill dir exists (`.claude/skills/grm-coding-practices-audit/`). | Passive skill file — lands via the normal file-merge; if MISSING after merge, run `workflow-bootstrap --restore` to restore it from `golden/`. No config change required (invoked on demand). | null |
| `github-issues` | v1.12 | External GitHub Issues tracker replaces/augments roadmap Backlog | Check `.claude/grimoire-config.json`: field `issue-tracker.trackers` exists and at least one entry has `provider` not equal to `roadmap`. | Ask the onboarding Step 6 question: "Choose your issue tracker: Roadmap (default) / GitHub." If GitHub is chosen, ask for `owner/repo`. Call `issue-tracker-switch set github <owner/repo>` with the captured repo. If the user chooses Roadmap, mark as declined (skip). | (optional, confirmed, backed-up, idempotent, reversible) Run `.claude/skills/grm-issue-tracker/migrate_roadmap_issues.py`. Contract: (1) **backup-first** — a timestamped copy of `docs/roadmap.md` is written to `.claude/cache/roadmap-backup-<ts>.md` (gitignored) before any mutation; (2) **--dry-run default** — invoke without `--apply` to preview all pending bullets; pass `--apply` to act; (3) **explicit confirmation** — `--apply` prompts "Migrate N bullet(s)?" and requires `yes` before proceeding, even under Noir; (4) **idempotent** — migration state in `.claude/cache/roadmap-migration-state.json` records each filed bullet by stable key; re-runs skip already-migrated bullets; (5) **reversible** — run with `--restore` to copy the latest backup back over `roadmap.md`; (6) each bullet is filed via `issue_tracker.py create` (the existing abstraction) with `audience: internal`; the bullet is removed from `roadmap.md` only after successful filing. |
| `execution-strategy` | v1.11 | Execution-strategy dial (Fast / Efficient / Cheap-Slow) for workflow dispatch | Check `.claude/grimoire-config.json`: `workflow-variant.value` is present and one of `{Fast, Efficient, Cheap-Slow}`. | Ask: "Choose your execution strategy: Fast / Efficient (default) / Cheap-Slow." Call `grm-workflow-variant-switch` with the chosen value (default `Efficient`). | null |
| `model-effort-profile` | v1.10 | Model/effort profile dial (cost posture) for agent dispatch | Check `.claude/grimoire-config.json`: `model-effort-profile.value` is present and a recognized profile name. | Ask: "Choose your model/effort profile: Medium (default) / High Effort / Low Effort / Efficient / Autonomous / Eco-Budget." Call `grm-model-effort-profile-switch` with the chosen value (default `Medium`). | null |
| `autonomous-push` | v1.16 | Opt-in unattended push for the Noir integration master (default human-gated). | Check `.claude/grimoire-config.json` for an `autonomous-push` block. | **Opt-in, security-sensitive — do NOT auto-adopt.** Leave absent (default human-gated). Only set `autonomous-push: { enabled: true }` on explicit user instruction; `push-guard.sh` rails (marker, allowlist, no destructive flags) still apply. | null |
| `cost-governance` | v1.15 | Cost-optimization config cluster: token budget + utilization reporting, per-agent verbosity, peak-hour scheduling policy, plus the `grm-priority-picker` advisor and the Steady Steward preset. | Check `.claude/grimoire-config.json` for a `cost-governance` block, and that the `grm-cost-budget` + `grm-priority-picker` skill dirs exist. | The skills land via the normal file-merge (restore via `workflow-bootstrap --restore` if MISSING). The `cost-governance` config block is **opt-in** — offer to run `grm-priority-picker` to populate it (or leave unset for default/unbudgeted behaviour). No schema-version bump (additive). | null |
| `agent-roles` | v1.14 | Expanded agent-role roster (Reviewer, Scout, Verifier, Triager, Researcher) + the `grm-install-doctor` skill, governed by the canonical registry `docs/grimoire/design/agent-roles-design.md`. | Check that the role skill dirs exist: `.claude/skills/grm-reviewer/`, `scout/`, `verifier/`, `triager/`, `researcher/`, and `install-doctor/` (all present → adopted). | These are passive skill files that land via the normal file-merge; if any are MISSING after merge, run `workflow-bootstrap --restore` to restore them from `golden/`. No config change required — the roles are spawned on demand by the integration master. | null |
| `paradigm-discoverability` | v1.22 | `CLAUDE.md` `## Paradigm` stamp + always-delivered `.claude/paradigms/README.md` breadcrumb index (names Supervised/Weiss/Noir + `grm-work-paradigm-switch`); `grm-sync-from-upstream` detects a stale `agentic-scaffolding` upstream and surfaces the `grimoire-framework` rename + paradigm system. | Check that `.claude/paradigms/README.md` exists AND `CLAUDE.md` contains a `> **Paradigm:**` stamp line. | Run `grm-workflow-bootstrap` (or `--restore`): it delivers `.claude/paradigms/README.md` from `golden/` and fills/refreshes the `CLAUDE.md` `## Paradigm` stamp from `work-paradigm.value`. The rename notice fires automatically on the next `grm-sync-from-upstream` run (no-op once repointed). | null |
| `release-phase-model` | v1.23 | `release-phase-model` dial (`Default`/`Auto`) + `grm-release-phase-model-switch` skill selecting how the Noir integration master executes a release; `Auto` (Noir-only) routes the whole release through the write-capable Workflow tier. | Check `.claude/grimoire-config.json` for a `release-phase-model` block AND that the `grm-release-phase-model-switch` skill dir exists (`.claude/skills/grm-release-phase-model-switch/`). | The skill lands via the normal file-merge (restore via `workflow-bootstrap --restore` if MISSING). The config dial is **additive and optional** (absent ⇒ `Default`) — offer to set it via `grm-release-phase-model-switch` (only `Default` unless the project runs Noir). No schema-version bump. | null |
| `web-app-aura-adoption` | v1.24 | Default way web apps adopt Aura: a layout/app-shell tier in `grm-design-language-adapt` (`docs/design/ux/layout.md`), a populated `grm-quick-start-template` "web" profile (`.claude/quick-start-templates/web/`), and the `docs/web-app-aura-adoption-guide.md` adoption guide. | Check that `.claude/quick-start-templates/web/template.json` exists AND `grm-design-language-adapt` emits a `layout.md` tier (skill contains a Step 3.5-C / `layout.md` reference). | Passive skill + template files land via the normal file-merge (restore via `workflow-bootstrap --restore` if MISSING). The web template applies on demand via `grm-quick-start-template`; the layout tier is emitted by `grm-design-language-adapt` for web/GUI stacks. Aura artifacts come from the design-language repo (`rhohn94/design-language#26`) at `vendor/aura`. No config change required. | null |
| `grm-component-registry` | v1.25 | Versioned component registry (`.claude/component-registry.json`) + `grm-component-registry` skill + controlled `component-taxonomy.md` + compatibility matrix (`.claude/cache/component-compatibility.json`); supersedes the v1.21 flat-scan export (which becomes a view). | Check that the `grm-component-registry` skill dir exists (`.claude/skills/grm-component-registry/`). | Passive skill + taxonomy doc land via the normal file-merge (restore via `workflow-bootstrap --restore` if MISSING). Build/refresh the registry by running `grm-component-registry`; the registry itself is a recognized 3-way-merged sync artifact (local components preserved). Regenerate the compatibility matrix after a sync changes the registry. No config change required. | null |
| `merge-gate-quality` | v1.26 | Config-gated `code-quality` cluster (audit-gate / auto-reviewer / coverage-threshold / typecheck) consulted by `grm-release-phase-merge` before ticking §5; blocks or warns on new standards gaps, reviewer findings, coverage misses, and type errors at the merge boundary. | Check `.claude/grimoire-config.json` for a `code-quality` block. | The gate logic lands via the normal file-merge in `grm-release-phase-merge` (restore via `workflow-bootstrap --restore` if MISSING). The `code-quality` config block is **additive and optional** (absent ⇒ defaults `warn`/`noir`/`null`/`build`) — set `{typecheck/lint/coverage}` commands via `grm-workflow-bootstrap` to activate the deterministic gates. No schema-version bump. | null |
| `grm-dependency-audit` | v1.27 | `grm-dependency-audit` skill: language-dispatched dependency vulnerability scanner (pip-audit/npm audit/cargo audit/govulncheck) emitting a normalized findings report; `--file-issues` files via `grm-feedback-to-issue`, `--fail-at` gates a release. | Check that the `grm-dependency-audit` skill dir exists (`.claude/skills/grm-dependency-audit/`). | Passive skill file — lands via the normal file-merge (restore via `workflow-bootstrap --restore` if MISSING). Invoked on demand; no config change required. | null |
| `grm-code-health` | v1.27 | `grm-code-health` skill: two-section report (dead code + duplication; complexity + maintainability with a baseline delta in `.claude/cache/code-health-baseline.json`); `--accept` rebaselines, `--gate` warns/blocks via the v1.26 `code-quality` dials. | Check that the `grm-code-health` skill dir exists (`.claude/skills/grm-code-health/`). | Passive skill file — lands via the normal file-merge (restore via `workflow-bootstrap --restore` if MISSING). Run on demand; rebaseline with `--accept`. No config change required. | null |
| `tooling-tier` | v1.27 | Deterministic linter/formatter + pre-commit tier documented in `docs/coding-standards/tooling.md`; quick-start templates ship per-profile lint/format config and (opt-in) a pre-commit config reusing the captured `{lint/typecheck/coverage}` commands. | Check that `docs/coding-standards/tooling.md` exists. | Passive doc + template config land via the normal file-merge. Set the `{lint-command}` etc. via `grm-workflow-bootstrap`; opt into the pre-commit install. No config change required. | null |
| `grm-doc-assurance` | v1.28 | `grm-doc-assurance` skill + `doc_assurance.py`: deterministic checks over the repo's own docs (design-doc layout, internal-link integrity, a validated `docs/README.md` map, cross-doc release consistency, and more). Framework-monorepo checks (flavor-parity, manifest-detect-hygiene, shipped-pointers) are automatically skipped on a single-flavor consumer project. Read-only except `--write-map`; `--strict` is a closeout gate. | Check that the `grm-doc-assurance` skill dir exists (`.claude/skills/grm-doc-assurance/`) AND `docs/README.md` (the docs map) exists. | Passive skill + script land via the normal file-merge (restore via `workflow-bootstrap --restore` if MISSING). Generate the docs map with `python3 .claude/skills/grm-doc-assurance/doc_assurance.py docs-map --write-map`; run the checks on demand or at release closeout. No config change required. | null |
| `context-efficiency` | v1.29 | Context/token-cost conventions: a 12 KB SKILL.md / 10 KB CLAUDE.md size budget enforced by the `grm-doc-assurance` `skill-budget` check (progressive disclosure — lean head + `reference.md`); cache-friendly stable→volatile context ordering + phase summaries; shared-context fan-out dispatch; and a per-release token baseline (`.claude/cache/token-baseline.json`) compared at closeout. | Check that `grm-doc-assurance` lists a `skill-budget` check. | Passive docs + the extended `grm-doc-assurance` script land via the normal file-merge. Run `grm-doc-assurance skill-budget` to see over-budget skills; capture the token baseline via `grm-token-measure` at closeout. No config change required. | null |
| `push-audit-log` | v1.30 | `push-guard.sh` appends an append-only approval record to `.claude/cache/push-audit.log` each time it permits a push (timestamp + command); all existing rails (marker, allowlist, denied destructive flags) unchanged. | Check `push-guard.sh` contains an `audit_log(` function. | The hook lands via the normal file-merge (restore via `workflow-bootstrap --restore` if MISSING). The log is written automatically on a permitted push; gitignore `.claude/cache/`. No config change. | null |
| `autonomy-hardening` | v1.30 | Unattended-dispatch guidance (#35 isolation checks) + `branch_cleanup.py` (safe `-d` / `-D`-candidate classification) + tiered merge-conflict resolution + transient-failure retry/backoff posture, documented in `grm-integration-master` + `grm-release-phase-merge`. | Check that `branch_cleanup.py` exists in the `grm-integration-master` skill dir (`.claude/skills/grm-integration-master/branch_cleanup.py`). | Passive script + docs land via the normal file-merge. Run `branch_cleanup.py` (dry-run) before deleting branches. No config change required. | null |
| `quickstart-profiles` | v1.31 | New quick-start templates (`service`, `gui`, `lib`) + a per-language `languages` variant block on each `template.json` (python/typescript/go/rust), matched by profile × declared language at apply time. | Check that `.claude/quick-start-templates/service/template.json` exists (and `gui`, `lib`). | Passive template files land via the normal file-merge (restore via `workflow-bootstrap --restore` if MISSING). Apply on demand via `grm-quick-start-template`. No config change required. | null |
| `grm-config-validate` | v1.31 | `grm-config-validate` skill + `config_validate.py`: schema validation (known blocks + value-sets + cross-rules) and idempotent `--migrate` (fills additive defaults atomically) for `grimoire-config.json`; called by `grm-install-doctor`. | Check that the `grm-config-validate` skill dir exists (`.claude/skills/grm-config-validate/`). | Passive skill + script land via the normal file-merge. Run `grm-config-validate` to check the config; `--migrate` to backfill additive defaults. No config change required. | null |
| `issue-label-taxonomy` | v1.31 | Recommended default label/audience taxonomy (`docs/grimoire/design/issue-label-taxonomy.md`) + onboarding Step 6 opt-in seeding (GitHub trackers only, idempotent, no-op for roadmap) + a `grm-priority-picker` one-pick intent→dial mapping. | Check `.claude/skills/grm-issue-tracker/issue_tracker.py` contains `def ensure_label`. | Doc lands via the normal file-merge; onboarding offers seeding on next run (GitHub trackers only). Idempotent. No config change required. | null |
| `stealth-mode` | v3.0 | Stealth Mode: orthogonal `stealth-mode` config dial (schema 3→4) that leaves zero AI/agent fingerprints in committed artifacts — ephemeral local-only context (`.git/info/exclude`, never `.gitignore`) + `stealth-guard.sh` commit/push guard, comment+commit-message hygiene deny-list (subsumes the `Co-Authored-By` rule), style-mirroring-over-standards with relaxed merge gates, categorical no-push, branch-baseline + mandatory reconciliation (suppressed `version/*` model); `grm-stealth-mode-switch` skill + `stealth_scrub.py`. | Check `.claude/grimoire-config.json` for a `stealth-mode` block AND that the `grm-stealth-mode-switch` skill dir exists (`.claude/skills/grm-stealth-mode-switch/`) AND `.claude/hooks/stealth-guard.sh` exists. | The skill + `stealth_scrub.py` + `stealth-guard.sh` hook land via the normal file-merge (restore via `workflow-bootstrap --restore` if MISSING); `config-validate --migrate` backfills the `stealth-mode` block (default `off`) and raises `schema-version` to 4. **Security-sensitive — never auto-activate**; turning stealth ON is an explicit `grm-stealth-mode-switch` action that records the ephemeral-context acknowledgement. | null |
| `grm-github-pr` | v3.5 | GitHub PR & review integration: opt-in `grm-github-pr` config block (`enabled`/`boundary`/`merge-method`/`review.{auto-dispatch,post-comments}`, no schema bump) + `github_pr.py` helper that opens a PR (idempotent `gh pr create`) and merges VIA the PR (`gh pr merge`) at a release boundary; the Reviewer gains a config-gated PR mode that reads the PR diff and posts findings to the PR; `push-guard.sh` widens the ref allowlist to `version/*` PR-head branches ONLY when `github-pr.enabled` (marker + destructive-flag + human-gate unchanged). Composes with the GitHub tracker, PM lanes, and autonomous-push; suppressed under Stealth. | Check `.claude/grimoire-config.json` for a `grm-github-pr` block AND that the `grm-github-pr` skill dir exists (`.claude/skills/grm-github-pr/` with `github_pr.py`). | The skill + `github_pr.py` land via the normal file-merge (restore via `workflow-bootstrap --restore` if MISSING); `config-validate --migrate` backfills the `grm-github-pr` block (default `enabled:false` ⇒ no behaviour change). Turning it ON is a deliberate edit; enabling on Copilot also requires adding `version/*` to `git-hooks/push-allowlist`. No schema bump. | null |
| `grm-environment-manager` | v3.4 | Environment-manager agent role (#76): own-session role for running + managing project instances — process/port inspection via `env_probe.py` (lsof/ss/ps → structured JSON), app launch with access-URL capture, stop/kill (per-action authorized — destructive-op gate in all paradigms), portable launch-script generation, and Reporter dispatch as the only issue-write path. No git writes; concurrency-safe with integration sessions. Model tier: sonnet/medium interactive, haiku/low inspection-only. | Check that the `grm-environment-manager` skill dir exists (`.claude/skills/grm-environment-manager/` with `SKILL.md` + `env_probe.py`). | Passive skill + script land via the normal file-merge (restore via `workflow-bootstrap --restore` if MISSING). Run `env_probe.py --self-test` to verify. No config change required. | null |
| `grm-status-broker` | v3.3 | Status-broker agent role (#73): strictly read-only "what is the status of X?" role with an ordered lookup (issue tracker → docs → code-last), backed by `project_status.py` (#74) — a deterministic stdlib-only reader that emits a JSON project overview from `grimoire-config.json` / `version-history.md` / `roadmap.md` / feature-manifest / package manifests. Ships with the scripting-unification research note (#75) recommending Python 3 stdlib-only as the framework scripting standard. | Check that the `grm-status-broker` skill dir exists (`.claude/skills/grm-status-broker/` with `SKILL.md` + `project_status.py`). | Passive skill + script land via the normal file-merge (restore via `workflow-bootstrap --restore` if MISSING). Read-only role; run `project_status.py --self-test` to verify. No config change required. | null |
| `sync-permissions-allowlist` | v3.2 | Scoped `permissions.allow` allowlist in `.claude/settings.json` (#72) pre-authorizing the framework's own maintenance scripts (`sync-from-upstream.sh`, `sync-from-source.sh`, `Write(.scaffold-upstream.conf)`) so an agent runs them unattended without the auto-mode classifier re-prompting — path-scoped, never a blanket grant; the guard hooks stay the safety net. Pairs with the corrected fork-safe `.scaffold-upstream.conf` provenance seeding (canonical `grimoire-framework.git`) and the `sync-from-upstream.sh` self-corruption fix (#71). | Check `.claude/settings.json` for a `permissions.allow` array containing an entry matching `sync-from-upstream.sh`. | **User-confirmed** (it grants the running agent capability): present the exact block (see `docs/grimoire/design/sync-reliability-design.md` §3 / `grm-workflow-bootstrap` Step 2 §settings.json) and merge the scoped entries into the live `permissions.allow` on approval, skipping entries already present (idempotent). `install-doctor --repair` step 6 retrofits a pre-v3.2 project. | null |
| `grm-iterate` | v3.11 | `iterate on {facet}` quota-driven improvement loop (#80): a skill that critically audits one facet (ux/code-quality/performance/security/backend/test-coverage/documentation/…), files issues until a T-shirt-size quota floor is met (XXL/XL/L/M/SM/XS), runs a full release cycle on them, and repeats (`x N` / `until-clean`). Composes the existing release skills (never reimplements them); push to origin stays human-gated in every paradigm. Backed by `iterate_quota.py` for deterministic quota math + resumable `.claude/iterate-state.json` (gitignored). Additive `grm-iterate` config block (`quota`/`per-facet`/`default-iterations`/`min-issues-floor`/`audit-agent`), no schema bump. Uses port isolation (#77) + recipe interface (#79) in its execution phase and a dispatched audit agent (#70-style). | Check that the `grm-iterate` skill dir exists (`.claude/skills/grm-iterate/` with `SKILL.md` + `iterate_quota.py`). | Passive skill + script land via the normal file-merge (restore via `workflow-bootstrap --restore` if MISSING); `config-validate --migrate` backfills the `grm-iterate` block (default quota XXL1/XL3/L5/M10/SM10/XS20, 1 iteration, floor 3, dispatched audit). Gitignore `.claude/iterate-state.json`. Seed `size:*` labels on first invocation. Run `iterate_quota.py --self-test` to verify. No schema bump. | null |
| `mcp-issue-tracker` | v3.12 | Bundled issue-tracker **MCP server** + the reusable stdlib **MCP runtime** it is built on (`.claude/mcp-servers/lib/mcp_runtime.py` — the `McpServer` base class, the template future Grimoire MCP servers subclass). The server (`.claude/mcp-servers/issue-tracker/server.py`) exposes the `issue_tracker.py` engine as 8 native MCP tools (list/get/search/create/comment/update/close/label) over newline-delimited stdio JSON-RPC — **zero third-party dependencies**, compact body-on-demand responses, post-mutation flush. Registered via a project-root `.mcp.json` (`grimoire-issue-tracker`); additive default-on `mcp` config block (`enabled`/`prefer-for-tracker`); tracker consumers re-pointed MCP-first (CLI fallback). Also adds a `comment` operation to the engine. Layout-agnostic across flavors (claude-code `.claude/mcp-servers/`, copilot `mcp-servers/`). | Check that `.mcp.json` contains `grimoire-issue-tracker` AND `.claude/mcp-servers/issue-tracker/server.py` exists. | Runtime + server land via the normal file-merge (restore via `workflow-bootstrap --restore` if MISSING); **merge** the `grimoire-issue-tracker` entry into any existing `.mcp.json` (read-merge-write — never clobber other `mcpServers`); `config-validate --migrate` backfills the `mcp` block (defaults `enabled:true`/`prefer-for-tracker:true`). Verify with `mcp_runtime.py --self-test` and `server.py --self-test`. No schema bump. | null |
| `grm-build-recipe` | v3.10 | Shared build-recipe interface (#79): a versioned named-target vocabulary (build/server/test/seed/migrate/lint/clean) + standard parameter contract, dispatched by a stdlib `recipe.py` over a per-project `.claude/recipes.json` (readable without executing, executed only via the dispatcher, sync extend-only — never overwrites an implemented target). Parameter resolution CLI>env>recipe>spec; the `server` target's `--port` defaults to `$GRIMOIRE_APP_PORT` (#77). Exit 0 only when the command ran; an unimplemented/unknown target exits 2 (never silent). `--generate <stack>` stubs inferrable targets. Consumed by environment-manager (#76), the QA agent (#70), and the CLAUDE.md commands table. | Check that the `grm-build-recipe` skill dir exists (`.claude/skills/grm-build-recipe/` with `recipe.py`). | Passive skill + script land via the normal file-merge (restore via `workflow-bootstrap --restore` if MISSING). Generate the per-project recipe with `recipe.py --generate <stack>` (server/cli/library) — extend-only, preserves implemented targets. Run `recipe.py --self-test` to verify. No config change required (the recipe file is self-describing via `interface-version`). | (extend-only) When the synced `INTERFACE_VERSION` is newer than the project's `.claude/recipes.json`, re-run `recipe.py --generate <stack>`: new interface targets are stubbed, existing implemented targets preserved unchanged. |
| `grimoire-footprint-gitattributes` | v3.8 | Footprint-reduction quick win (#78): a `.gitattributes` rule block that marks Grimoire framework-internal docs `linguist-documentation` (out of GitHub's language bar, kept browsable) and agent-only operational artifacts (`docs/release-planning-*.md`, `docs/grimoire/qa-ledger.md`, `docs/grimoire/**`, research spikes) `export-ignore` (excluded from `git archive` tarballs) — **suppress-without-moving**, so ZERO impact on `grm-sync-from-upstream` 3-way merge / golden / bootstrap. Companion research note `docs/grimoire/design/footprint-reduction-design.md` scopes the (migration-required) relocations as follow-ups. | Check that `.gitattributes` exists AND contains a line matching `docs/release-planning-` with `linguist-documentation`. | **Idempotent merge** — append the Grimoire rule block (see `claude-code/.gitattributes`) to the project's `.gitattributes`, skipping any rule line already present; a project's own `.gitattributes` rules are preserved. No file moves, no config change, no schema bump. | null |
| `worktree-ports` | v3.7 | Unique-port-per-worktree allocation (#77): `claim_port.py` hands each parallel worktree a unique, pre-verified free TCP port BEFORE any app launch — closing port-conflict AND silent test-poisoning (an agent's traffic hitting a sibling's instance). Strategies os-assign (default, bind-to-0)/random-probe/index; idempotent per worktree-id via gitignored `.claude/cache/port-claims.json`; probes-but-doesn't-hold (the app binds); exit 1 fatal. The port is exposed as `GRIMOIRE_APP_PORT` (configurable), claimed in `grm-worktree-preflight` and consumed by `grm-environment-manager`. Additive `worktree-ports` config block (`enabled`/`strategy`/`range-start`/`range-end`/`env-var`), no schema bump. | Check that `claim_port.py` exists in the `grm-worktree-preflight` skill dir (`.claude/skills/grm-worktree-preflight/claim_port.py`) AND `.claude/grimoire-config.json` has a `worktree-ports` block. | Passive script lands via the normal file-merge (restore via `workflow-bootstrap --restore` if MISSING); `config-validate --migrate` backfills the `worktree-ports` block (defaults `os-assign`/`20000`–`29999`/`GRIMOIRE_APP_PORT`). Run `claim_port.py --self-test` to verify. No schema bump. | null |
| `grm-qa-agent` | v3.6 | QA agent role (#70): own-session role that retrospectively verifies SHIPPED features against their original acceptance criteria — selects a release window (default earliest-unverified) via `qa_select.py` reading `docs/grimoire/qa-ledger.md` (the QA-state ledger) + `version-history.md` + each `release-planning-vX.Y.md` §5 Status Ledger, checks each feature against its acceptance criteria + referenced design docs, and files shortcomings to the tracker via `grm-feedback-to-issue` (deduped, config-gated). No git writes; the tracker is its only write surface; the dispatching master records the verdict row. Distinct from the Verifier (pre-merge gate). Additive `qa` config block (`window-mode`/`window-size`/`verify-depth`/`auto-file-findings`), no schema bump. | Check that the `grm-qa-agent` skill dir exists (`.claude/skills/grm-qa-agent/` with `SKILL.md` + `qa_select.py`). | Passive skill + script land via the normal file-merge (restore via `workflow-bootstrap --restore` if MISSING); `config-validate --migrate` backfills the `qa` block (defaults `earliest-unverified`/`1`/`acceptance`/`true`). Seed `docs/grimoire/qa-ledger.md` with the project's shipped releases (status `unverified`) to scope the QA program. Run `qa_select.py --self-test` to verify. No schema bump. | null |
| `grm-project-manager` | v3.1 | Project Manager agent role atop a three-tier hierarchy (PM → integration master → task agents): tracks components, runs overlap analysis (`pm_overlap.py`) to partition features into non-colliding lanes, dispatches one integration master per lane (parallel under Noir/Claude-Code; serial on Copilot/Stealth), integrates the lanes, and gates the release on per-feature QA (Verifiers). The integration master is narrowed to one feature lane (backward-compatible — standalone for a single-feature release). Additive `grm-project-manager` config block (`max-parallel`/`overlap-policy`/`qa-gate`), no schema bump. | Check that the `grm-project-manager` skill dir exists (`.claude/skills/grm-project-manager/` with `SKILL.md` + `pm_overlap.py`). | The paradigm-variant skill + `pm_overlap.py` land via the normal file-merge (restore via `workflow-bootstrap --restore` if MISSING); `grm-work-paradigm-switch` installs the active variant; `config-validate --migrate` backfills the `grm-project-manager` block (defaults `3`/`balanced`/`block`). No schema bump. Absent the block ⇒ no PM engaged ⇒ today's single-master behavior. | null |
| `integration-branch-integrity` | v3.38 | Single-integration-line invariant + divergence guard (#126): a model-aware **tree-content** divergence check in `release_plan.py` (and the `grimoire-release` `merge_preflight` tool) HALTs a promotion when `main` carries work the integration line lacks — without false-positiving on a healthy repo whose `main` is ahead only by promotion-merge commits; `grm-sync-from-upstream` + `grm-design-language-adapt` refuse to apply on `main`/non-integration branches except at a clean release boundary and emit framework-sync vs Aura vendoring as **separate commits**; `protected-branch-guard.sh` self-tests direct-commit-on-`main` denial; `integration-workflow.md` documents the invariant + merge-forward recovery. Additive `branch-model` config block (`integration-branch`, default `dev`), no schema bump. | Run `release_plan.py --self-test` (covers the divergence guard: healthy/benign-only/real-fork) AND `protected-branch-guard.sh --self-test` (covers direct-commit-on-`main`) — both pass. | Engine + guard + skill edits land via the normal file-merge (restore via `workflow-bootstrap --restore` if MISSING); `config-validate --migrate` accepts the optional `branch-model.integration-branch` key (default `dev`). Verify with the two self-tests above; confirm `integration-workflow.md` carries the single-integration-line invariant. No schema bump. | null |
| `generated-golden-image` | v3.49 | The golden image (Grimoire's pristine reference set) is **generated, not committed**: a new `generate_golden.py` (in `grm-workflow-bootstrap`) derives it from the flavor/install by a declared transform (path-flatten + exclude the 5 authoring-only skills + junk filter + an operational seed supplement). The former committed `grm-workflow-bootstrap/golden/` tree is removed; restore/audit consumers (`grm-install-doctor`, `grm-regenerate-grimoire`, `grm-hard-reset`) resolve it via `resolve_golden()` (extract a frozen `.grimoire-golden/golden-v{X.Y}.tar.gz` archive, else generate from a flavor — never from a customized tree); the release build attaches a checksummed `golden-v{X.Y}.tar.gz` asset; `grm-workflow-snapshot` is retired to a deprecation stub. | Check that `generate_golden.py` exists in the `grm-workflow-bootstrap` skill dir (`.claude/skills/grm-workflow-bootstrap/generate_golden.py`) AND that the legacy committed tree `.claude/skills/grm-workflow-bootstrap/golden/` is ABSENT. | The `generate_golden.py` helper + `seed/` supplement land via the normal file-merge (restore via `workflow-bootstrap --restore` if MISSING); on sync, delete any leftover committed `grm-workflow-bootstrap/golden/` tree and gitignore `.grimoire-golden/`. Freeze a local baseline with `generate_golden.py --freeze .` (bootstrap Step 0 does this automatically). Run `generate_golden.py --self-test` to verify. No config change required. | Delete the obsolete committed `.claude/skills/grm-workflow-bootstrap/golden/` tree if a pre-v3.49 sync left it in place; add `.grimoire-golden/` to `.gitignore`. The golden baseline regenerates on demand thereafter. |
| `standard-project-structure` | v3.50 | Standard project file-structure contract: every Grimoire project follows one canonical top-level layout — `src/` + `tests/`, `lib/first-party/` (your own reusable libraries) + `lib/third-party/` (vendored external deps; the `grm-sync-deps` target, replacing any top-level `vendor/`), plus `scripts/`/`dist/`/`config/`/`assets/`/`tools/`/`examples/` as needed. The contract is documented in `docs/project-structure.md`; the machine-readable rules live in the `structure` block of `.claude/architecture-rules.json` (required dirs, aliases, git-ignored outputs), enforced by `grm-architecture-audit` as a fitness function (`structure-*` findings). A new `grm-structure-migrate` skill classifies layout findings (MISSING_REQUIRED / NONSTANDARD_DIR / TRACKED_OUTPUT / VENDOR_DEST) and with `--apply` relocates them via `git mv` and rewrites `vendor.toml` dest — never deleting work. | Check that the `grm-structure-migrate` skill dir exists (`.claude/skills/grm-structure-migrate/SKILL.md`) AND `.claude/architecture-rules.example.json` contains a top-level `structure` block. | The `grm-structure-migrate` skill + the `structure` block in `architecture-rules.example.json` land via the normal file-merge (restore via `workflow-bootstrap --restore` if MISSING); copy the `structure` block into the project's own `.claude/architecture-rules.json` if you maintain one, then run `grm-structure-migrate` (report) followed by `grm-structure-migrate --apply` to bring the tree to the standard. Verify with `grm-architecture-audit` (no `structure-*` findings). No schema bump. | Relocate `vendor/`→`lib/third-party/` and `test/`→`tests/` via `git mv`, rewrite `vendor.toml` dest to `lib/third-party/`, and git-ignore build output — all performed by `grm-structure-migrate --apply` (explicitly confirmed; never deletes). |
| `grm-workspace-clean` | v3.52 | Workspace-hygiene cleanup (#185): a new `grm-workspace-clean` skill — a tiered report-then-apply broom (house style, like `grm-structure-migrate`) that classifies each known Grimoire workspace artifact KEEP / SAFE-DELETE and removes ONLY the safe-delete set (`.design-language-source/`, `.grimoire-golden/`, `.grimoire-source/`, `.scaffold-sync-backup/`) behind a hard denylist that unconditionally refuses `.claude/` and `.scaffold-base/` (load-bearing). Report-only by default; `--apply` deletes; never deletes without the flag. Companions: `.scaffold-base/` and `.design-language-source/` are now gitignored across all flavors (closing the v3.51 Lane G gap — clutter that was neither tracked nor ignored), and `grm-sync-from-upstream` + `grm-regenerate-grimoire` self-clean their transient `.scaffold-sync-backup/`/staging on a successful run. Backed by `workspace_clean.py` with `--self-test`. No schema bump. | Check that the `grm-workspace-clean` skill dir exists (`.claude/skills/grm-workspace-clean/` with `SKILL.md` + `workspace_clean.py`). | The skill + `workspace_clean.py` land via the normal file-merge (restore via `workflow-bootstrap --restore` if MISSING); the gitignore additions for `.scaffold-base/` and `.design-language-source/` merge into the project's `.gitignore` (section-merge, never removing existing rules). Run `workspace_clean.py --self-test` to verify, then `grm-workspace-clean` (report) and optionally `--apply`. No config change required. No schema bump. | null |
| `standard-justfile-recipes` | v3.53 | Justfile contract: `build`, `run`, and `deploy` recipes with standard argument signatures, as defined by the v3.53 Justfile standard. Every Grimoire project with a `justfile` must expose these three named recipes (without a `grimoire:placeholder` marker) so the standard build-recipe interface and `grm-install-doctor` can invoke them deterministically. Design authority: `docs/design/justfile-standard-design.md`. | Check that the project's `justfile` (or `Justfile`) contains `build`, `run`, and `deploy` recipe definitions AND does NOT contain a `grimoire:placeholder` marker on those recipes. | Add the three standard recipes (`build`, `run`, `deploy`) per the contract in `docs/design/justfile-standard-design.md`; implement each recipe body for the project's stack (replace the stub/placeholder). Run `grm-install-doctor` to verify the justfile shows an OK status for this feature. | null |
| `codex-flavor` | v3.55 | Grimoire Codex flavor: a new first-class `codex/` flavor (parallel to `claude-code/` and `copilot/`) targeting the OpenAI Codex CLI, ported high-fidelity from the canonical claude-code workflow. Ships `codex/AGENTS.md` (the always-on instruction file, ≤ 32 KiB), `.codex/config.toml` (sandbox/approval + `[features]`/`[memories]`/`[agents]` enabling Codex's built-in memory pipeline + multi-agent), `.codex/hooks.json` with portable Python protected-branch/push/release-plan guards + native `git-hooks/`, `.codex/agents/` (integration-master/reporter/project-manager) resolved via `/agent`, `.codex/profiles/` (Supervised/Weiss/Noir via `codex --profile`), `prompts/` (10 procedure docs) + `scripts/` (`issue_tracker.py` port + a five-check `config_validate.py`), a full `docs/` tree, and `docs/design/codex-flavor-design.md`. `grm-doc-assurance`/`grm-config-validate` are now four-flavor aware and `build_distributables` auto-discovers `codex` via its `.grimoire-flavor` marker. Design authority: `codex/docs/design/codex-flavor-design.md`. | Check that the top-level `codex/` flavor directory exists with its `codex/.grimoire-flavor` marker AND `codex/AGENTS.md`. | The `codex/` flavor ships as a self-contained distributable (its own `.zip` from `build_distributables`); Codex CLI users copy the `codex/` tree into their project and follow `codex/README.md` (install git hooks via `bash codex/git-hooks/install.sh`, trust the project, pick a paradigm via `codex --profile <name>`). claude-code/copilot consumers need no action — the flavor is additive and opt-in. No schema bump. | null |
| `copilot-grm-namespacing` | v3.54 | Copilot-flavor `grm-` namespacing migrate engine: `copilot/scripts/grm_namespacing.py` gives copilot consumers the same bare-name → `grm-` cutover engine the claude-code flavor already ships, completing the namespacing rollout across flavors. It renames only real `.claude/skills/` survivors (never the `.github/prompts/*.prompt.md` slash-commands), is idempotent, archives-before-removing on post-sync collisions, and has a `--dry-run` mode and a `--self-test`. A `/namespacing-migrate` prompt is the consumer entry point. Applies to copilot-flavor projects only. Design authority: `docs/design/copilot-grm-namespacing-design.md`. | Check (copilot-flavor projects) that `copilot/scripts/grm_namespacing.py` exists OR, on a project that IS the copilot flavor, that `scripts/grm_namespacing.py` exists. | The engine + `/namespacing-migrate` prompt land via the normal file-merge (restore via `workflow-bootstrap --restore` if MISSING). Run `scripts/grm_namespacing.py --self-test` to verify, then `scripts/grm_namespacing.py --dry-run` to preview and `--apply` to complete the bare-name → `grm-` cutover. No config change required. No schema bump. | Run `scripts/grm_namespacing.py --apply` (explicitly confirmed; archives-before-removes, never silently deletes) to rename any leftover bare-name `.claude/skills/` survivors to the `grm-` prefix. |
