# Grimoire Framework upstream (seeded by workflow-bootstrap v1.13+; overridable).
# Forks: replace the URL below with your own upstream before committing.
# Idempotency: workflow-bootstrap will not overwrite this if UPSTREAM_REPO is already set.
UPSTREAM_REPO=https://github.com/rhohn94/grimoire-framework.git
UPSTREAM_REF=main
# FLAVOR is auto-detected from this project's layout (.claude/ → claude-code,
# .github/ → copilot). Add FLAVOR=claude-code (or copilot) only to override
# an ambiguous detection.
