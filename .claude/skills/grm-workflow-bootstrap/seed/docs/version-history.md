# Version History

> The **complete internal record** of shipped releases of **this project**,
> newest first — one section per tagged version. This is the engineering
> ledger: it may cross-reference ticket/issue IDs, internal task names, and
> technical detail, because it is never shown to end users. The **front-facing**
> equivalent — clean, user-voice release notes with no process leakage — lives
> in `changelog.md`; write both when you cut a release. Forward-looking plans
> live in `roadmap.md`. This file is **yours** — Grimoire seeds the empty
> template and never writes its own framework changelog here.

<!-- Add a new "## vX.Y — <title>" section per release, newest first. -->
