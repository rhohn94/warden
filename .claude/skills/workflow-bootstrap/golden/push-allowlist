# Project push allowlist (read by .claude/hooks/push-guard.sh).
#
# Default allowlist (always allowed when the integration marker is present):
#   main, dev, and any version tag matching v?\d+(\.\d+){0,3}(-...)?
#
# Add one ref name per line below to extend the default allowlist for this
# project (e.g. additional protected branches, named release/* lines).
# `#` starts a comment. Blank lines are ignored.
#
# Examples (uncomment / adapt):
#   release/legacy-1.x
#   dev-staging
